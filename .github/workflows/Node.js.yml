name: Node.js CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x]

    env:
      SESSION_ID: 𝙰𝚂𝙸𝚃𝙷𝙰-𝙼𝙳=3jYjCRKa#xjzHaKZkqUeN_V4nzkfibqcsXcev-fOz5ecpykK3G1Q
      GITHUB_AUTH_TOKEN: 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
      GITHUB_USERNAME : Apiyasinha

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install dependencies
      run: npm install

    - name: Start application
      run: npm start
